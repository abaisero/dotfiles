[user]
  email = andrea.baisero@gmail.com
  name = abaisero
[alias]
  br = branch
  co = checkout
  st = status
  l = log --graph --decorate --pretty=format:'%C(auto)%h%d %C(yellow)(%C(blue)%as:%an%C(yellow))%C(auto) %s'
[color]
  ui = true
[log]
  stat = true
  graph = true
[core]
	editor = nvim
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
