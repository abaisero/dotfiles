send-keys 'cd $HOME/git/cv/academia' C-m
send-keys 'vim cv-baisero.tex' C-m

split-window -v -c "#{pane_current_path}"
send-keys 'cd $HOME/git/cv/academia' C-m
send-keys 'latexmk -pvc'

new-window -c "#{pane_current_path}"
send-keys 'cd $HOME/git/cv/industry' C-m
send-keys 'vim cv-baisero.tex' C-m

split-window -v -c "#{pane_current_path}"
send-keys 'cd $HOME/git/cv/industry' C-m
send-keys 'latexmk -pvc'
