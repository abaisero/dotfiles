send-keys 'i3-rename-workspace crl' C-m

send-keys 'cd $HOME/git/notes/notes-crl/' C-m
send-keys 'vim notes.typ' C-m

new-window -c "#{pane_current_path}"
send-keys 'cd $HOME/git/notes/notes-crl/' C-m
send-keys 'just' C-m

split-window -v -c "#{pane_current_path}"
send-keys 'cd $HOME/git/notes/notes-crl/' C-m
send-keys 'o notes.pdf' C-m
